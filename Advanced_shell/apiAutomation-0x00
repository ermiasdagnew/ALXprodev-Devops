#!/bin/bash
URL="https://pokeapi.co/api/v2/pokemon/pikachu"
OUTPUT="data.json"
ERROR_LOG="errors.txt"

curl -s -f "$URL" -o "$OUTPUT"
if [ $? -ne 0 ]; then
    echo "$(date) - Failed to fetch Pikachu data" >> "$ERROR_LOG"
    exit 1
fi
echo "Pikachu data saved to $OUTPUT ✅"
